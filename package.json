{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "frontend": " rm -rf dist && mkdir dist && cd frontend && npm run build && mv dist/* ../dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.6.1",
    "express": "^5.2.1",
    "mongodb": "6.7",
    "node-json-db": "^2.6.0",
    "path": "^0.12.7",
    "sanitize-filename": "^1.6.4"
  }
}
